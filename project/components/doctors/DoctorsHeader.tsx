'use client';

interface DoctorsHeaderProps {
  title: string;
  subtitle: string;
  resultCount: number;
  onOpenMobileFilter: () => void;
}

export default function DoctorsHeader({
  title,
  subtitle,
  resultCount,
  onOpenMobileFilter
}: DoctorsHeaderProps) {
  return null;
}